<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Embedded SignUp</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            color: #444444;
            margin: 0;
            padding: 0;
        }
        .signup-container {
            min-height: 85vh;
            max-width: 500px;
            margin: 50px auto;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
        }
        .form-header {
            text-align: center;
            margin-bottom: 1 rem;
            font-size: 1.5rem;
            font-weight: bold;
            color: #181818;
        }
        .form-group label {
            font-weight: bold;
            margin-bottom: 4px;
        }
        .form-control {
            border-radius: 5px;
            border: 1px solid #ccc;
            padding: 8px;
        }
        .form-control:focus , .form-select:focus {
            border-color: #0176d3;
            outline: none;
            box-shadow: 0 0 0 1px #0176d3;
        }
        .btn-primary {
            background-color: #0176D3;
            border-color: #0176D3;
            padding: 10px 20px;
            border-radius: 5px;
            transition: background-color 0.3s ease, box-shadow 0.3s ease;
        }
        .btn-primary:hover {
            background-color: #0056b3;
            box-shadow: 0 4px 10px rgba(0, 123, 255, 0.3);
        }
        .indent-group {
            margin-left: 2px;
            position: relative;
        }
        .indent-group::before {
            content: "*";
            position: absolute;
            left: -5px;
            top: -5px;
            font-size: 1.2rem;
            color: #df2e2e;
        }
        
        @media (max-width: 768px) {
            .signup-container {
                margin: 20px;
                padding: 20px;
            }
            .form-header {
                font-size: 1.2rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="signup-container bg-white">
            <div class="form-header">
                <h2>Embedded Sign Up</h2>
            </div>
            
            <form id="embeddedSignupForm" action="/create-account" method="GET">
                <div class="mb-3">
                    <label class="form-label">Solution Id</label>
                    <input type="text" class="form-control" name="solutionId" required>
                </div>
                
                <div class=" mb-3 indent-group">
                    <label class="form-label">Config Id</label>
                    <input type="text" class="form-control" name="configId">
                </div>
                
                <div class="mb-3">
                    <label class="form-label">App Id</label>
                    <input type="text" class="form-control" name="appId" required>
                </div>
                
                <div class="mb-3">
                    <label class="form-label">Version</label>
                    <select class="form-select" name="version" required>
                        <option value="">Select Version</option>
                        <option value="1.0">1.0</option>
                        <option value="2.0">2.0</option>
                        <option value="3.0">3.0</option>
                    </select>
                </div>
                
                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-primary">Sign Up</button>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>







<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Embedded SignUp</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .signup-container {
            max-width: 600px;
            margin: 50px auto;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
        }
        .form-header {
            text-align: center;
            margin-bottom: 30px;
        }
        .indent-field {
            margin-left: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="signup-container bg-white">
            <div class="form-header">
                <h2>Embedded SignUp</h2>
            </div>
            
            <form id="embeddedSignupForm" action="/create-account" method="GET">
                <div class="mb-3">
                    <label class="form-label">Solution Id</label>
                    <input type="text" class="form-control" name="solutionId" required>
                </div>
                
                <div class="mb-3 indent-field">
                    <label class="form-label">Config Id</label>
                    <input type="text" class="form-control" name="configId">
                </div>
                
                <div class="mb-3">
                    <label class="form-label">App Id</label>
                    <input type="text" class="form-control" name="appId" required>
                </div>
                
                <div class="mb-3">
                    <label class="form-label">Version</label>
                    <select class="form-select" name="version" required>
                        <option value="">Select Version</option>
                        <option value="1.0">1.0</option>
                        <option value="2.0">2.0</option>
                        <option value="3.0">3.0</option>
                    </select>
                </div>
                
                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-primary">Sign Up</button>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> -->