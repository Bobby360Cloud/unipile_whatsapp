<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup Successful</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .success-container {
            max-width: 800px;
            margin: 50px auto;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
        }
        .success-header {
            text-align: center;
            margin-bottom: 30px;
        }
        .success-header img {
            height: 50px;
            margin-bottom: 15px;
        }
        .credentials {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .api-details {
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="success-container bg-white">
            <div class="success-header">
                <img src="https://www.karix.io/wp-content/uploads/2022/02/karix-logo.svg" alt="Karix Logo">
                <h2>Signup Successful!</h2>
                <p class="text-success">Your account has been successfully created with Karix Messaging Hub.</p>
            </div>
            
            <div class="credentials">
                <h4>Account Details</h4>
                <p><strong>Username:</strong> <%= username %></p>
                <p><strong>Email:</strong> <%= email %></p>
                <p><strong>Mobile:</strong> <%= mobile %></p>
            </div>
            
            <div class="api-details">
                <h4>API Credentials</h4>
                <div class="alert alert-warning">
                    <strong>Important:</strong> Save these credentials securely. They will not be shown again.
                </div>
                
                <div class="mb-3">
                    <label class="form-label">API Key</label>
                    <input type="text" class="form-control" value="<%= apiKey %>" readonly>
                </div>
                
                <div class="mb-3">
                    <label class="form-label">Messaging Endpoint</label>
                    <input type="text" class="form-control" value="<%= messagingEndpoint %>" readonly>
                </div>
                
                <div class="alert alert-info">
                    <h5>Next Steps:</h5>
                    <ul>
                        <li>Use the API Key and Messaging Endpoint to send messages</li>
                        <li>Configure your webhook endpoints to receive messages and delivery reports</li>
                        <li>Refer to the API documentation for implementation details</li>
                    </ul>
                </div>
            </div>
            
            <div class="d-grid gap-2">
                <a href="/signup" class="btn btn-primary">Create Another Account</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>